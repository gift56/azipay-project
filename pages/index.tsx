import { HeroSection } from "@/components";
import MainLayout from "@/layouts/MainLayout";

const Homepage = () => {
  return (
    <MainLayout>
      <HeroSection />
    </MainLayout>
  );
};

export default Homepage;
